# Infrastructure Glossary

- **Docker Compose**: Tool for defining and running multi-container Docker applications locally.
- **Environment Tiers**: Distinct deployment targets (`local`, `development`, `staging`, `production`, `air-gapped`) defined in the infrastructure structure.
- **Named Volumes**: Docker mechanism for persisting data across container lifecycles, used in local development.
