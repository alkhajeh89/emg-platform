# ADR-038 §6.3 EMG Delegation Profile — Capability Matrix

All 10 mandatory properties, each with real executable evidence from the
isolated verification environment. Full pass/fail output in
`05_TEST_EXECUTION_LOG.md`. Every test below was actually run against the
running `emg-adr038-verification-keycloak` container in this session.

| # | Property | Evidence (file :: test) | Result | Note |
| :-: | :--- | :--- | :-: | :--- |
| 1 | Authenticated Human Principal preservation | `test_positive_delegation.py::test_human_subject_preserved` | **PASS** | Exchanged credential's `sub` equals the originating human token's `sub`, verified through the independent downstream validator. |
| 2 | Independently identifiable Acting Service | `test_positive_delegation.py::test_acting_service_independently_identifiable` | **PASS** | `azp` carries the Acting Service (`emg-verification-bff`), cryptographically bound inside the signed credential, and provably distinct from `human_subject`. Determined experimentally that Keycloak 25 uses `azp`, not `act` — see `02_KEYCLOAK_VERIFICATION_CONFIG.md` Finding 1. |
| 3 | Single-audience restriction | `test_positive_delegation.py::test_audience_a_accepts_its_own_credential`, `::test_audience_b_accepts_its_own_credential`; `test_negative_delegation.py::test_2_cross_audience_credential_reuse_is_rejected`, `::test_10_service_only_token_is_not_treated_as_a_delegated_credential` | **PASS** | A credential issued for Audience A is accepted by A and independently rejected by B (`DownstreamValidationError`). A bare Acting-Service client-credentials token (never exchanged) carries no downstream `aud` at all and is rejected by every audience validator — no service-only fallback exists (Security Invariant 9.10). |
| 4 | Scope reduction only | `test_positive_delegation.py::test_scope_equal_or_reduced`; `test_negative_delegation.py::test_1_privilege_elevation_via_requested_subject_is_rejected` | **PASS** | Exchanged credential's `realm_access.roles` is always a subset of the originating Human Principal's own roles. A lower-privileged human (B) cannot obtain A's higher clearance via `requested_subject`; the resulting credential (when Keycloak issues one at all) still reflects B's own, lower attributes. |
| 5 | Tenant integrity | `test_positive_delegation.py::test_tenant_unchanged`; `test_negative_delegation.py::test_3_tenant_substitution_via_client_input_is_ignored` | **PASS** | `tenant_id` in the exchanged credential always equals the originating human's own `tenant_id`. A client-supplied `tenant_id` form field in the exchange request is silently ignored, never honoured. |
| 6 | Classification-clearance integrity | `test_positive_delegation.py::test_clearance_unchanged`; `test_negative_delegation.py::test_4_clearance_elevation_via_client_input_is_ignored` | **PASS** | Same pattern as tenant integrity, for `classification_clearance`. |
| 7 | Confidential-client authentication | `test_negative_delegation.py::test_9_exchange_without_valid_client_authentication_is_rejected`, `::test_5_unauthorized_acting_service_is_rejected` | **PASS** | A wrong or missing `client_secret` is rejected (401) before any exchange logic runs. A structurally-valid confidential client that authenticates correctly but was never granted exchange permission is rejected (403 `access_denied`) — confidentiality alone is necessary but not sufficient; explicit grant is required and enforced. |
| 8 | Independent downstream Delegated Credential validation | `test_positive_delegation.py::test_downstream_validation_is_independent_per_audience`; `test_negative_delegation.py::test_6_invalid_issuer_is_rejected`, `::test_8_altered_signature_is_rejected` | **PASS** | Two separately-constructed validator instances (mirroring production's `TenantServiceTokenValidator` contract field-for-field) each independently verify signature/issuer/audience/expiry against the same JWKS, with no shared state and no shortcut — a genuinely valid, correctly-signed credential is still rejected against the wrong issuer, and a one-character signature flip is rejected. |
| 9 | Bounded Delegated Credential lifetime | `test_positive_delegation.py::test_credential_lifetime_bounded`; `test_credential_lifetime.py::test_configured_lifetime_matches_recorded_value`, `::test_repeated_exchange_each_credential_independently_bounded_not_extended`, `::test_reexchanging_a_delegated_credential_does_not_extend_beyond_realm_ceiling`; `test_negative_delegation.py::test_7_expired_delegated_credential_is_rejected` | **PASS** | Configured value: **60s**, applied uniformly and confirmed by decoding a real issued credential. A real (not simulated) wait past expiry is rejected. Repeated exchange never extends or accumulates lifetime; attempting to re-exchange an already-issued credential is rejected outright by Keycloak (`400 invalid_token`) — see `02_KEYCLOAK_VERIFICATION_CONFIG.md` Finding 5. |
| 10 | Complete audit attribution | `test_positive_delegation.py::test_delegated_credential_carries_both_identities_for_audit`; `test_audit_attribution.py::test_audit_event_attributes_both_human_principal_and_acting_service`; `test_negative_delegation.py::test_11_audit_unavailable_blocks_rather_than_silently_succeeding` | **PASS** | The real, unmodified `emg_audit_service` app durably persists one event carrying both `source_principal` (server-assigned from the Acting Service's own verified service token, never client-controlled — the existing Sprint 6 security-review guarantee, re-verified here not re-implemented) and `actor`/`actor_type` (the Human Principal), independently and distinguishably. Simulated audit-store outage produces a ≥500 response, never a silent 200 — fail-closed per ADR-036 D-9. |

**25 of 25 executable tests pass** (10 positive delegation + 11 negative + 3 dedicated credential-lifetime + 1 audit-attribution integration = 25). No property required a workaround that weakened an ADR-038 invariant. No property could not be satisfied.
