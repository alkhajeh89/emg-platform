# Explicit Limitations

1. **Phase 10 (new epoch continuation) was established structurally, not by performing a new production-style genesis.** The decisive trial and restart matrix already prove: (a) the old, terminated epoch/operation cannot continue (its zero-value/terminated state permits no authority-dependent action); (b) `NewEpochRequired()` evaluates true; (c) the old unresolved operation's Spanner row remains non-authoritative for witness reconstruction (no witness was ever, or could ever be, produced for it). A genuinely new epoch would simply use fresh identifiers — exactly the pattern every other test in this qualification (and Wave 2 Track F's real genesis) already follows, never reusing an old operation ID, revision, or witness key. A separate, full new-genesis run was judged unnecessary to additionally prove this and was not performed, per the explicit instruction not to automatically perform one unless necessary for the S7 proof.
2. **Signing used a local, test-only ECDSA key, not real Cloud KMS** — an explicit, documented scoping decision (see `README.md`), identical to Wave 1 Track A's own real-cloud test. The real KMS/TLS/WIF signing boundary remains separately, already qualified (Tracks C/D/E/F).
3. **No Kubernetes, WIF, or IAM topology was provisioned** — Phase 4 explicitly calls for the minimum topology necessary, and the S7 code path (`completeRawCommit`/`buildCommittedPayload`/`CreateExactIfAbsent`) requires none of them. The operator's own `gcloud`-authenticated session supplied real Cloud Spanner/GCS credentials, identical to the technique Track A's own real-cloud test already used and had reviewed.
4. **Attacks D, E, H, and I in the attack matrix were classified out-of-scope for a new real-cloud trial** rather than re-tested, each with an explicit reason and pointer to where the underlying property is already covered (unit-tested `epoch` package logic, or Wave 2 Track F's already-real-cloud-qualified genesis idempotency). This is disclosed plainly in `ATTACK_MATRIX.md`, not silently assumed.
5. **The separate GCS ambiguous-write gap (Wave 2 Track F's disclosed "Gap B") remains open and is not addressed by this qualification.** S7, as authoritatively defined, is specifically about Spanner-side process loss (ADR-044 §17 item 7); it does not claim to close every possible ambiguous-provider-error case across every subsystem.
6. **Provider fact-check gaps**: several official-documentation fetch attempts this session did not surface a directly on-topic passage for specific narrow claims (see `PROVIDER_FACT_CHECK.md`) — reported honestly rather than treated as confirmed, with an explanation of why this codebase's architecture does not depend on that specific external confirmation to remain correct.
7. **This qualification does not establish production administrative independence, does not provision production infrastructure, and does not authorize S7's completion to mean any later phase (S8/production rollout) is authorized.** `PRODUCTION_ADMINISTRATIVE_INDEPENDENCE_RESULT` remains `NOT ESTABLISHED`, unchanged by this task.
